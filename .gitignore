node_modules/
dist/
.next
*.log
.DS_Store

# Secrets and local env (never commit)
.env
.env.local
.env.*.local

# TypeScript incremental build info (regenerated by tsc)
*.tsbuildinfo

# Cursor debug session logs (if present)
.cursor/debug*.log
